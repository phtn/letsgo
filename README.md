# Letsgo! - Go Project Initializer

### usage

```zsh

letsgo <project_name>

```

### run and build

Read the `Makefile` for shorcut commands when developing.

For example you can run and watch your go project by:

```zsh

make r

```
